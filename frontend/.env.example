# Neon PostgreSQL 연결 문자열 (서버 전용)
DATABASE_URL=

# 입장 게이트 코드 (서버 전용)
ACCESS_CODE=

# Vercel Blob 파일 저장소 토큰 (서버 전용)
# https://vercel.com/dashboard/stores → Blob 생성 후 ".env.local" 탭에서 복사
BLOB_READ_WRITE_TOKEN=
